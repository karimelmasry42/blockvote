node_modules

# npm lockfile — this project is yarn-only. Ignore any stray package-lock.json
# produced by an accidental `npm install` so it never gets re-committed.
package-lock.json

# dependencies, yarn, etc
# yarn / eslint
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions
.eslintcache
.DS_Store
.vscode
.idea
.vercel
packages/hardhat/deployed-contracts*
.claude

*.log
yarn-debug.log*
yarn-error.log*
